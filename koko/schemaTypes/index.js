import product from "./product"
import banner from "./banner"
import review from "./review" // Import the new review schema

export const schemaTypes = [ product, banner, review] // Add review to the array
