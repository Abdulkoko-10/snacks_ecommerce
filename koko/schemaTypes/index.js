import product from "./product"
import banner from "./banner"
import review from "./review" // Import the new review schema
import preorder from "./preorder" // Import the new preorder schema

export const schemaTypes = [ product, banner, review, preorder] // Add preorder to the array
